<ui:FluentWindow x:Class="MonitorBrightness.UI.FlyoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MonitorBrightness.UI"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Brightness"
        WindowStyle="None"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="Height"
        MinHeight="0"
        Width="300"
        Deactivated="Window_Deactivated"
        Closing="Window_Closing">
    <ui:FluentWindow.Resources>
        <local:NullOrEmptyToVisibilityConverter x:Key="NullOrEmptyToVis"/>
    </ui:FluentWindow.Resources>
    <Grid Margin="16,14">
        <StackPanel>
            <TextBlock Text="Brightness" FontSize="13" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="2,0,0,10"/>
            <ItemsControl x:Name="MonitorsItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,14">
                            <Grid Margin="2,0,2,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding DisplayName}"
                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                           FontSize="12" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Grid.Column="1" Text="{Binding Brightness, StringFormat={}{0}%}"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                           FontSize="11" Margin="8,0,0,0"/>
                            </Grid>
                            <Slider Minimum="0" Maximum="100"
                                    Value="{Binding Brightness, Mode=OneWay}"
                                    IsMoveToPointEnabled="True"
                                    SmallChange="1" LargeChange="5"
                                    ValueChanged="Slider_ValueChanged"
                                    Tag="{Binding}"/>
                            <TextBlock Text="{Binding StatusNote}"
                                       Foreground="{DynamicResource SystemFillColorCautionBrush}"
                                       FontSize="10" Margin="2,4,2,0" TextWrapping="Wrap"
                                       Visibility="{Binding StatusNote, Converter={StaticResource NullOrEmptyToVis}}"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock x:Name="EmptyStateText" Text="No controllable displays detected."
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
